# Database
DATABASE_URL=postgresql://user:password@hostname/database?sslmode=require

# Redis (for BullMQ + rate limiting)
REDIS_URL=redis://localhost:6379

# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_OAUTH_REDIRECT=http://localhost:3001/api/auth/google/callback

# Meta OAuth
META_APP_ID=your-meta-app-id
META_APP_SECRET=your-meta-app-secret
META_OAUTH_REDIRECT=http://localhost:3001/api/auth/meta/callback

# Gemini AI (server-side only)
GEMINI_API_KEY=your-gemini-api-key

# Token Encryption (32 bytes, base64 encoded for AES-256-GCM)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
ENCRYPTION_KEY=your-base64-encoded-32-byte-key

# Server
PORT=3001
NODE_ENV=development

